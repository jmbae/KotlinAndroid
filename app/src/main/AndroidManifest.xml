<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.coderails.kotlinandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".O01HelloActivity" />
        <activity android:name=".O02ClickEventActivity" />
        <activity android:name=".O03InnerClassEventActivity" />
        <activity android:name=".O04MenuAndIntentActivity" />
        <activity android:name=".O05PassAndReturnDataActivity" />
        <activity android:name=".O05ReceiveDataActivity" />
        <activity android:name=".O06BookActivity" />
        <activity android:name=".O07AsyncActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>